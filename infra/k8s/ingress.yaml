apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ech-0278
  namespace: ech-0278
  annotations:
    kubernetes.io/ingress.class: gce
spec:
  rules:
    - host: ech-0278.gap-labs.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
